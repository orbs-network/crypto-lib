version: 2
jobs:
  build-linux:
    machine: true
    steps:
      - checkout
      - run: ./.circleci/setup-rngd.sh
      - run: docker build -f Dockerfile.sdk.base -t orbs:sdk-base .
      - run: docker build -f Dockerfile.sdk -t orbs:sdk .

workflows:
  version: 2
  build:
    jobs:
      - build-linux
